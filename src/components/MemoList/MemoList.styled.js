import styled from "styled-components";

const StDiv = styled.div`
  padding: 20px 10px;
  overflow-y: scroll;
  &::-webkit-scrollbar {
    display: none; /* Chrome, Safari, Opera*/
  }
`;

export { StDiv };
